#include "main.h"
/**
*_abs - check for absolute of an interger
*@n : parameter to deternmine return
*Return: return absolute number depending on condintion
*/
int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
}
